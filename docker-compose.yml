version: "3.8"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: devops-app
    ports:
      - "8001:3000"
    restart: always
    environment:
      NODE_ENV: production